.col-sm-6.col-sm-offset-3
  #pics.transitions-enabled
    - @pics.each do |pic|
      .box.panel.panel-default
        = link_to (image_tag pic.image.url, class: 'img-responsive center-block'), pic
        %h2= link_to pic.title, pic
